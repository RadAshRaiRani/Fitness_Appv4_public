import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Turbopack config for Next.js 16 (webpack config removed since we're using relative paths)
  turbopack: {},
};

export default nextConfig;
